# so i was unable to compile this into one project,so i did the code for characristics of Cae 1,2 and 3, respectively
class Car:
def _init_(self, color,type,year)
   self.color= "red"
   self.type= "TOYOTA corolla sedan"
   self.year = "1999"

   def start(self)
   return f' "this is" {self.color}{self.type}"from the year"{self.year}'

class Car2:
def _init_(self, color,type,year)
   self.color= "white"
   self.type= "TOYOTA HILUX"
   self.year = "2010"

   def start(self)
   return f' "this is" {self.color}{self.type}"from the year"{self.year}'
class Car3:
def _init_(self, color,type,year)
   self.color= "blue"
   self.type= "Honda CRV"
   self.year = "2007"

   def start(self)
   return f' "this is" {self.color}{self.type}"from the year"{self.year}'
